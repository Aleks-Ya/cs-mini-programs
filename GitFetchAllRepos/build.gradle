apply plugin: 'java'
sourceSets.main.java.srcDir 'src'
group = "ru.yaal.project"
version = "1.1-SNAPSHOT"
jar {
    manifest {
        attributes(
                "Main-Class": "GitFetchAllRepos"
        )
    }
}